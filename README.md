# 録 roku

Write a script. Play it back as a terminal session.

```
roku play demo.roku
```

roku is a Rust CLI for scripting and replaying terminal sessions. Describe keystrokes, timing, and output in a plain `.roku` file. roku does the rest. No screen capture, no video files. Just text in, terminal out.

> **録** (*roku*) — to record.

## Status

Early days — the parser and playback engine are still taking shape.

| | |
|---|---|
| `.roku` file parsing | In progress |
| `roku play` | In progress |
| `roku rec` | Planned |
| GIF / video export | Planned |

## Install

```bash
cargo install --path .
```

## Usage

```bash
roku play demo.roku
```

## The `.roku` format

`.roku` files are human-readable scripts that describe a terminal session. They're meant to be written by hand or generated by `roku rec`.

```text
# demo.roku

Set Shell    zsh
Set Output   demo.gif
Set Pace     250ms

Sleep 1s
Type@250ms "echo hello"
Enter
Hide
Show
```

### Directives

| Directive | Description |
|---|---|
| `Set Shell <shell>` | Shell to use (`zsh`, `bash`, `fish`, …) |
| `Set Output <file>` | Output file path (e.g. `demo.gif`) |
| `Set Pace <duration>` | Default typing pace between keystrokes |
| `Sleep <duration>` | Pause for a duration (`1s`, `500ms`, …) |
| `Type@<pace> "<text>"` | Type text at an optional per-command pace |
| `Enter` | Press the return key |
| `Hide` | Hide terminal output |
| `Show` | Show terminal output |

Durations follow Rust's convention: `ms` for milliseconds, `s` for seconds.

## Roadmap

- [ ] Playback engine (`roku play` fully working)
- [ ] GIF/video export
- [ ] Recording (`roku rec`) — capture a live session into a `.roku` file

## Etymology

**録** is the kanji for *record* or *録音 (rokuon)* (sound recording). The romanized form is `roku`, which is short, typeable, and gives clean subcommands: `roku rec`, `roku play`. The name earns its meaning.

## License

MIT
